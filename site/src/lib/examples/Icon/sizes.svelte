<script lang="ts">
    import { Icon, icons } from '@facile/muse';
</script>

<div class="flex items-center gap-4 text-fc-fg">
    <Icon icon={icons.settings} size={16} />
    <Icon icon={icons.settings} size={18} />
    <Icon icon={icons.settings} size={24} />
</div>
