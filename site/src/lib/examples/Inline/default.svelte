<script lang="ts">
    import { Button, icons, Inline } from '@facile/muse';
</script>

<Inline gap="tight">
    <Button icon={icons.plus}>Nouveau projet</Button>
    <Button variant="outline" icon={icons.download}>Exporter</Button>
    <Button variant="ghost" icon={icons.filter}>Filtrer</Button>
</Inline>
